name: ipapi_dart
description: Dart bindings for ipquery.io | A free and performant ip address API 
version: 0.0.1
homepage: https://github.com/MultiX0/ipapi-dart

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  dio: ^5.8.0+1
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  mockito: ^5.4.0
  build_runner: ^2.3.0
